<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kavya.radiojaiho">


    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutUsActivity"
            android:label="@string/about_us"/>
        <activity android:name=".ShoutBoxActivity"
            android:label="@string/shout_box"/>
        <activity android:name=".LiveRadioActivity"
            android:label="@string/live_radio"/>
        <activity android:name=".RequestSongActivity"
            android:label="@string/request_song"/>
        <activity android:name=".ContactUsActivity"
            android:label="@string/contact_us"/>
    </application>

</manifest>